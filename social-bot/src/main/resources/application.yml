server:
    port: 8080
    
spring:
    jackson:
         property-naming-strategy: SNAKE_CASE
    activemq:
        broker-url: "stomp+nio://localhost:61616?trace=true&wireFormat=stomp&persistent=false&useJmx=false&useShutdownHook=true"